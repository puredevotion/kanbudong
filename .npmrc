# HeroUI v3 and react-native both lean on peer ranges that pnpm's default strict
# resolution rejects; auto-install-peers keeps a fresh clone from needing a
# manual peer-dependency archaeology session.
auto-install-peers=true
strict-peer-dependencies=false
